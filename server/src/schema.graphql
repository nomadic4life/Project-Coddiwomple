 type Query {
  Users: [User!]!
  User(id: ID!): User!
}

type Mutation {
  createUser(username: String!, password: String!): User!
  connectUser(username: String!, password: String!): User
}

type User {
  id: ID!
  username: String!
  password: String!
}